TODO
====

- Keep the direct Makefile in sync with the active encoder, frontend, and
  decoder-support source lists.
- Keep CI coverage aligned with supported build modes:
  - library-only build
  - command line frontend build
  - install staging
  - smoke test
- Review config.h periodically and remove settings that only supported deleted
  build systems or deleted platform wrappers.
- Consider whether the command line frontend should remain the default target
  or whether the library-only target should become the primary consumer path.
- Keep README.md, INSTALL, USAGE, HACKING, and doc/man/lame.1 consistent with
  the maintained Makefile-based build.
